i httpd.yml    //httpd.yml playbook name

- hosts: localhost
  tasks:
   - package:
       name: httpd
       state: absent
       
ansible-playbook httpd.yml   //to run the playbook to uninstall the package of httpd(apache) service
